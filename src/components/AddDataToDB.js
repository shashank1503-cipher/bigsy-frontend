import { Flex, Heading, Text } from "@chakra-ui/react";
import React from "react";

const AddDataToDB = () => {
  return (
    <Flex>
      <Flex direction={"column"} textAlign={"center"}>
        <Heading>Add Data To DB</Heading>
        <Text> Add Various Types of Data</Text>
      </Flex>
    </Flex>
  );
};

export default AddDataToDB;
